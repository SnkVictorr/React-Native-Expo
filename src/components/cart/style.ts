import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  image: {
    width: 80,
    height: 80,
    resizeMode: "contain",
    alignSelf: "center",
    marginTop: 50,
  },
});
