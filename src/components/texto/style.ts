import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title : {
    fontSize: 40,
    marginTop: 40,
    color: "#000000ff",
    marginVertical: 8,
    textAlign: "center",
    marginLeft: 10,
    fontWeight: "semibold"
    
  },
});
