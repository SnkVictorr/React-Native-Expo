import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  label: {
    fontSize: 16,
    color: "#dabb3fff",
    marginVertical: 8,
    textAlign: "left",
    marginLeft: 10,
    
  },
});
