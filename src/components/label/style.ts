import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  label: {
    fontSize: 20,
    color: "#333",
    marginVertical: 8,
    textAlign: "left",
    marginHorizontal: 16,
  },
});
