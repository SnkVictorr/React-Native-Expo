import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    height: 40,
    borderColor: "gray",
    fontSize: 16,
    borderWidth: 1,
    color: "#333",
    margin: 10,
    paddingHorizontal: 10,
    borderRadius: 5,
  },
});
