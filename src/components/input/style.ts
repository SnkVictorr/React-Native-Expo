import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    height: 40,
    borderColor: "gray",
    borderRadius: 12,
    borderWidth: 1,
    margin: 10,
    paddingHorizontal: 10,
    fontSize: 16,
    color: "#ececec",
  },
  focused: {
    borderColor: "#c7a315",
    borderWidth: 2,
  },
});
